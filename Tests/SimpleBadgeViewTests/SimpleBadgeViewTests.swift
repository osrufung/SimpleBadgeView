import XCTest
@testable import SimpleBadgeView

final class SimpleBadgeViewTests: XCTestCase {
    func testExample() {
        XCTAssertEqual(SimpleBadgeView().count, 0)        
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
